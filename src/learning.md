@Entity is used to make the logical representation of the database
@Id this annotations makes the id property a primary key of our table
if there is entity annotations is already present over ther then why we need 
table annotations questions arises cause they both have the name annotations 
inside?
ans - table annotations only works over the db and entity annotations only 
works on the java logic code 